{
    "tasks": [
        {
            "label": "Atone: Build (debug)",
            "type": "shell",
            "command": "make",
            "args": ["DEBUG=1"],
            "options": {
                "cwd": "${workspaceFolder}",
            },
            "problemMatcher": [ "$gcc" ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
        }
    ],
    "version": "2.0.0"
}