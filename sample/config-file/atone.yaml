#!/usr/bin/env atone -c

settings:
    workdir: .

services:
    sleep-3:
        command: sleep 3

    sleep-5:
        schedule:
            - '0 02 * * *'
        command: sleep 5
        depends_on: [ sleep-3 ]

    bash:
        command: bash -c ls

    tracer: ./tracer.sh -arg1 -arg 2
